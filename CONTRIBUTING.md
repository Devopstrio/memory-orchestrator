# Contributing to Memory Orchestrator

1. Fork the repo and create your branch from `main`.
2. Ensure you've run `make install-dev` to get all tools.
3. Run `make lint` and `make test` before submitting your PR.
4. Fill out the PR template completely.
